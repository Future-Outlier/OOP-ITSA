#include <bits/stdc++.h>
using namespace std;

int main()
{   
    double down, h;
    while (cin >> down >> h) {
        cout << fixed << setprecision(1) << down * h / 2 << '\n';
    }
    return 0;
}
